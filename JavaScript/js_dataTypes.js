let a = 5 ;
let b = "5"
let c = "7"

console.log(a==b)
console.log(a==c)
console.log(a===b)
console.log(b===c)

// = --> store the data into the variable
// == --> it checks the contains of the variable but not checks the type 
// === -->  it checks the contains of the variable but  checks the type 

let firstname = "pronoy"
let firstName = "ayush"

console.log(firstName);

// why js caseSensitive language


let fn = "pronoy"
let ln = "kundu"

console.log(fn+" "+ln)